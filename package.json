{
  "name": "check_links_api",
  "version": "0.8",
  "description": "This project aims to build an API for testing links present in a text document, which will validate the existence of the site and if it is still online",
  "main": "index.js",
  "type": "module",
  "dependencies": {
    "chalk": "^5.3.0",
    "fetch": "^1.1.0"
  },
  "scripts": {
    "cli": "node cli.js",
    "cli:valid": "node cli.js ./files/text.md --valid"
  }
}
